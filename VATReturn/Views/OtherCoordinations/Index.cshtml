@model IEnumerable<VATReturn.Models.OtherCoordination>

@{
    ViewBag.Title = "Index";
}

<h2>অন্যান্য সমন্বয়করণ</h2>
<br />
@{
    var insId = 0;

    if (ViewBag.massage != null)
    {
        insId = ViewBag.massage;
    }
    else
    {
        foreach (var data in Model)
        {
            insId = data.InstitutionInfo.Id;
            break;
        }
    }

    <table>
        <tr>
            <td><p>@Html.ActionLink("যোগ করুন", "Create", new { id = insId })</p></td>
            <td>&nbsp; &nbsp;</td>
            <td><p>@Html.ActionLink("পরবর্তী ধাপ", "Index", "ReturnCalculation", new { id = insId }, null)</p></td>
        </tr>
    </table>
}
<br />
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.InstitutionInfo.TaxId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OtherRebates)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Owing)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SourceCut)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Blank)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateTime)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.InstitutionInfo.TaxId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OtherRebates)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Owing)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SourceCut)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Blank)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateTime)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
